# Add an order switching button

Currently the top level items are in alphabetical order, but it might make sense to also offer ordering by the amount of todos
and maybe even making that the default.
